<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 해당 파일에 모든 쿼리문을 작성 -->
<!-- namespace에는 xml파일과 연결할 Mapper interface 파일명을 패키지를 포함하여 적어주세요 -->
<mapper namespace="com.green.greenshare.message.mapper.DMMapper">


    <!-- 메시지 전송 -->
    <insert id="sendMessage" parameterType="DirectDTO">
        INSERT INTO messages (thread_id, sender, receiver, content, timestamp)
        VALUES (#{threadId}, #{sender}, #{receiver}, #{content}, #{timestamp})
    </insert>

    <!-- 특정 스레드의 메시지 조회 -->
    <select id="getMessagesByThread" resultType="DirectDTO">
        SELECT thread_id, sender, receiver, content, timestamp
        FROM messages
        WHERE thread_id = #{threadId}
    </select>

    <select id="LastByThread" resultType="DirectDTO">
        SELECT thread_id, sender, receiver, content, timestamp
        FROM messages
        WHERE thread_id = #{threadId}
        <if test="lastMessageId != null">
            AND id &lt; #{lastMessageId}
        </if>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>


</mapper>































